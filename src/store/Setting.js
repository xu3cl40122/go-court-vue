
const state = () => ({
  setting: {}

})

const getters = {

}

const mutations = {
  setSetting: (state, setting) => state.setting = setting
}

const actions = {
 
}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}